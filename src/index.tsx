import * as React from 'react';
import * as ReactDOM from 'react-dom';
import {BrowserRouter} from 'react-router-dom';
import {AppRoutings} from './AppRoutings';
import './index.css';

ReactDOM.render(<AppRoutings />, document.getElementById('root'));