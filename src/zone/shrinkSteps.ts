/**
 * @interface
 * @description Interface for object, that contains steps counters
 */
export interface IShrinkSteps {
  topStep: number;
  bottomStep: number;
  leftStep: number;
  rightStep: number;
}
